[project]
name = "gameboard-trainer"
version = "0.1.0"
description = "Policy/value trainer utilities for gameboard-bot"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.2",
    "safetensors>=0.4",
    "numpy>=1.26",
    "packaging>=23",
]

[tool.uv]
package = false

[project]
name = "{{package_name}}"
version = "0.1.0"
description = "Enterprise ML pipeline scaffold."
requires-python = ">=3.11"
dependencies = ["PyYAML>=6.0.2"]

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

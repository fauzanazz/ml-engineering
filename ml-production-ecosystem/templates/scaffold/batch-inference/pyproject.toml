[project]
name = "{{project_name}}"
version = "0.1.0"
description = "Flexible ML scaffold."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

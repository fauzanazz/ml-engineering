# {{project_name}}

Enterprise ML pipeline scaffold with ingestion, validation, training, approval, batch inference, monitoring, and rollback notes.

## Commands

```bash
uv run python -m {{package_name}}.pipeline
uv run pytest
```
